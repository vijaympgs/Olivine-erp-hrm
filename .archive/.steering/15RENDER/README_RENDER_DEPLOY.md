# Render Deployment Guide — Olivine Retail ERP

This package contains everything required to deploy the current build
(Django + React/Vite + PostgreSQL) on Render.

Order:
1. Create PostgreSQL on Render
2. Deploy backend (Django)
3. Deploy frontend (Vite)
4. Configure env vars
5. Enable CI/CD
6. Verify health & HTTPS
